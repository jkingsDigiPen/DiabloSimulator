<UserControl x:Class="DiabloSimulator.UserControls.HeroEquipmentDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
						 Loaded="Window_Loaded">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="100"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition MinWidth="250"/>
			<ColumnDefinition MinWidth="250"/>
			<ColumnDefinition MinWidth="250"/>
		</Grid.ColumnDefinitions>
		<Label Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" 
									 FontSize="18" FontWeight="SemiBold">Equipment</Label>
		<ListBox Name="lbEquip0" Grid.Row="1" Grid.Column="0"></ListBox>
		<ListBox Name="lbEquip1" Grid.Row="1" Grid.Column="1"></ListBox>
		<ListBox Name="lbEquip2" Grid.Row="1" Grid.Column="2"></ListBox>
	</Grid>
</UserControl>
